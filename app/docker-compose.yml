version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
    restart: always
    networks:
      - learning-lab-net

# #comment when run root docker compose 
# networks:
#   learning-lab-net:
#     external: true
